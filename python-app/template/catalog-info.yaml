apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{values.app_name}}
  description: ${{values.app_name}}  ${{values.app_description}}
  annotations:
     github.com/project-slug: priti-devops-org-94/${{values.app_name}}
     backstage.io/techdocs-ref: dir:.
spec:
  type: service
  owner: development
  lifecycle: experimental
